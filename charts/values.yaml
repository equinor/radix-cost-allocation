# Default values for charts.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
image:
  repository: radixdev.azurecr.io/radix-export-cost
  tag: latest
  pullPolicy: Always

job:
  schedule: "0 * * * *"
  restartPolicy: Never

secret:
  name: sql-credential

db:
  user: keaaa
  database: keaaa-delete-me
  server: keaaa-delete-me.database.windows.net
  password: password

prometheus:
  api: http://prometheus-operator-prometheus.default:9090